# pi_mgmt_bot
a bot to manage my raspberry pi running all my other bots, a simple interface to shutdown, restart, get ip addrs to ssh.

# Create your venv
python3 -m venv .venv

# Activate it
source .venv/bin/activate

# Install all requirements
pip install -r requirements.txt